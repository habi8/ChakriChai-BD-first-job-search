{
  "name": "chakrichai-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless job-search aggregation API (BD-first). No database — proxies/scrapes on request only.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
